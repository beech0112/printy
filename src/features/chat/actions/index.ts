/**
 * Action registry — stubbed.
 * Flow-based action handlers removed with the flow engine.
 * Will be replaced by AI tool handlers in src/lib/ai/tools.ts.
 */
export const actionHandlers: Record<string, any> = {};
